# Landing TODO — outstanding work

Last updated: 2026-04-23

Unfinished items from the April 2026 landing rework. All blocked on real
assets (video / screenshots) or a stronger content decision — no code
work is blocking any of them, just content/brand choices.

---

## T5 — Case-study / proof link near hero

**Status:** attempted + removed.

Previously shipped a dev.to link under hero CTA (`Read: "Full SaaS UI in
31 minutes" →`). Removed 2026-04-23 — felt weak as hero-level proof on
its own.

**What would make it work:**
- Stronger venue than dev.to (HN front page, Product Hunt feature,
  tweet from a known voice, or a customer site you can point at) — or
- A dedicated `/case-studies/*` route with 2-3 real customer stories,
  and a "See case studies →" link that points there

**Decision needed:** which proof story goes in hero? Dev.to is fine as
a secondary link lower on the page (e.g., inside the FAQ answer "Is it
production-ready?"), but not as the hero's primary proof hook.

**Code placement if revived:** `app/page.tsx` — inside the hero CTA
`motion.div` (was `{...fade(3.5)}` block, same position).

---

## T10 — "What you get" after Getting Started commands

**Status:** attempted + removed (2026-04-23).

First attempt: static grid of 5 styled mini-pages (`SaaSOutput.tsx`).
Rejected as "looks topornо" — stylized cards still read as wireframe-ish
despite realistic text content. Same failure mode as the T12 attempt
(fake rendered UI doesn't beat real output).

**What would work:**
1. **Real screenshots** of an actual Coherent-generated project — ideally
   a deployed example with a public URL. Each page a PNG, put in
   `/public/case/*.png`, render as a grid of `<img>`s. Most honest.
2. **Animated short GIF / MP4** showing browser tab-switching across the
   5 pages of a real generated project — conveys that they share DS
   without requiring the reader to visually diff.
3. **Live iframe embed** of one showcase page (e.g., the landing's own
   `/design-system` route) with a label "this whole section was
   generated by Coherent" — honest, smallest scope.

**Decision needed:** is option (1) or (2) worth the production effort
right now, or is (3) a good "ship placeholder" while real case study
projects are built?

**Code placement if revived:** `app/page.tsx` — inside `§07 Getting
Started`, after the `$0.01–$0.03 cost` caption Reveal. Insertion point
is clean (no other edits needed).

---

## T12 — Money shot: cascade visual

**Status:** attempted + removed (2026-04-23).

First attempt: interactive `CascadeDemo` component (scoped CSS vars +
5 wireframe thumbnails + swatch picker). Rejected — abstract wireframes
undersell "design system cascade" and reduce the claim to "changing a
CSS variable", which is the most trivial browser feature.

**What would work:**
- **Video / GIF of a real `coherent chat` session** — split-screen of
  terminal + IDE (Cursor / Claude Code), user types something like
  `coherent chat "change accent to blue"`, pages recompile live and
  cascade the new token across the DS. 20-40 seconds. Silent loop.
  MP4 + WebM, ~500-900 KB, 1200 px wide.

  Hosting: `/public/cascade.mp4` + `/public/cascade.webm`, rendered as
  `<video autoplay loop muted playsinline>`.

**Decision needed:** record the session. Maybe paired with a short
caption block naming the components that cascaded.

**Code placement if revived:** `app/page.tsx` — between the `HERO`
section close and the `KILLER LINE` section (the same slot
`CascadeDemo` previously occupied, above the 3-line comparison block).

---

## T13 — Before / after drift visualization

**Status:** not attempted.

Plan called for a side-by-side showing "typical AI output drift" (left)
vs "Coherent output with shared foundation" (right). Legal / reputation
risk: "typical AI output" requires either (a) generating a weak UI
ourselves and framing it as generic AI output, which reads as
strawman, or (b) actual screenshots from named competitors, which is
legally sensitive and ages fast.

**What would work:**
- Ship a real competitor comparison ONLY with neutral labels
  ("Typical output" / "With Coherent") and genuinely neutral content —
  or drop the row in favor of the existing `§05 Compare` table, which
  already positions Coherent vs competitors without visual caricature.

**Recommendation:** consider this **superseded by `§05 Compare`**. The
table already delivers the "different tools, different goals" point
without the visual strawman risk. Only revive T13 if a stronger visual
case emerges.

---

## Smaller follow-ups

- **Stats-row number currency** — hero stats row says "1,068 tests
  passing, 23 enforced rules, 5 atmospheres". Confirm these match the
  CLI's current state before every release bump. Same numbers also
  appear in FAQ answer "Is it production-ready?" — keep them synced or
  wire to a shared constant.
- **FAQ "atmosphere engine faster" footer mention** (line ~833 in
  `app/page.tsx`) — decide if "atmosphere engine" as an internal
  codename belongs in user-facing copy.
- **GitHub stars threshold** — currently hidden if < 50 (in
  `app/page.tsx` stats row). Confirm this is the right floor or let it
  show earlier with a different framing.
